package xyz.nkomarn.composter.server

import xyz.nkomarn.composter.Composter

class ServerTickThread(private val server: Composter) : Thread() {

    override fun run() {
        while (true) {

        }
    }
}